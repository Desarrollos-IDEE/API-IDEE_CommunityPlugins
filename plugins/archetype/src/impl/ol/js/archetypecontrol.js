/**
 * @module IDEE/impl/control/ArchetypeControl
 */
export default class ArchetypeControl extends IDEE.impl.Control {
  /**
   * Esta función añade el control al mapa
   *
   * @public
   * @function
   * @param {IDEE.Map} map mapa donde se añadirá el plugin
   * @param {HTMLElement} html html del plugin
   * @api stable
   */
  addTo(map, html) {
    super.addTo(map, html);
  }
}
