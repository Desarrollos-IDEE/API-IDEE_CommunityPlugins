1. ERROR in ./node_modules/xml2js/lib/parser.js 17:17-47
Module not found: Error: Can't resolve 'timers' in '/home/carmenmarquez/proyectos/UNIFICACION/SUBIDA/plugins/API-IDEE_CommunityPlugins/plugins/catalogsearch/node_modules/xml2js/lib'

BREAKING CHANGE: webpack < 5 used to include polyfills for node.js core modules by default.
This is no longer the case. Verify if you need this module and configure a polyfill for it.

If you want to include a polyfill, you need to:
	- add a fallback 'resolve.fallback: { "timers": require.resolve("timers-browserify") }'
	- install 'timers-browserify'
If you don't want to include a polyfill, you can use an empty module like this:
	resolve.fallback: { "timers": false }
 @ ./node_modules/xml2js/lib/xml2js.js 12:11-30
 @ ./src/facade/js/catalogsearchcontrol.js 18:0-33 267:10-28 311:12-30
 @ ./src/facade/js/catalogsearch.js 18:0-58 118:26-46
 @ ./src/index-ol.js 1:0-170 3:32-54

Motivo: al cambiar de versión de Webpack ya no se incluye la librería timers.

Solución: 
- Instalar timers-browserify: npm install --save-dev timers-browserify
- Añadir en los ficheros Webpack dentro de fallback:
timers: require.resolve('timers-browserify'),


2. WARNING in ./node_modules/sax/lib/sax.js 193:13-37
Module not found: Error: Can't resolve 'stream' in '/home/carmenmarquez/proyectos/UNIFICACION/SUBIDA/plugins/API-IDEE_CommunityPlugins/plugins/catalogsearch/node_modules/sax/lib'

BREAKING CHANGE: webpack < 5 used to include polyfills for node.js core modules by default.
This is no longer the case. Verify if you need this module and configure a polyfill for it.

If you want to include a polyfill, you need to:
	- add a fallback 'resolve.fallback: { "stream": require.resolve("stream-browserify") }'
	- install 'stream-browserify'
If you don't want to include a polyfill, you can use an empty module like this:
	resolve.fallback: { "stream": false }
 @ ./node_modules/xml2js/lib/parser.js 9:8-22
 @ ./node_modules/xml2js/lib/xml2js.js 12:11-30
 @ ./src/facade/js/catalogsearchcontrol.js 18:0-33 267:10-28 311:12-30
 @ ./src/facade/js/catalogsearch.js 18:0-58 118:26-46
 @ ./src/index-ol.js 1:0-170 3:32-54

1 warning has detailed information that is not shown.
Use 'stats.errorDetails: true' resp. '--stats-error-details' to show it.

Motivo: al cambiar de versión de Webpack ya no se incluye la librería stream.

Solución: se comprueba que no se usa, aunque depende de la librería sax que a su vez depende de xml2js que sí que se usa, por lo que se puede desactivar.
Añadir en los ficheros Webpack dentro de fallback: stream: false,


3. ERROR in ./node_modules/safe-buffer/index.js 2:13-30
Module not found: Error: Can't resolve 'buffer' in '/home/carmenmarquez/proyectos/UNIFICACION/SUBIDA/plugins/API-IDEE_CommunityPlugins/plugins/catalogsearch/node_modules/safe-buffer'

BREAKING CHANGE: webpack < 5 used to include polyfills for node.js core modules by default.
This is no longer the case. Verify if you need this module and configure a polyfill for it.

If you want to include a polyfill, you need to:
	- add a fallback 'resolve.fallback: { "buffer": require.resolve("buffer/") }'
	- install 'buffer'
If you don't want to include a polyfill, you can use an empty module like this:
	resolve.fallback: { "buffer": false }
resolve 'buffer' in '/home/carmenmarquez/proyectos/UNIFICACION/SUBIDA/plugins/API-IDEE_CommunityPlugins/plugins/catalogsearch/node_modules/safe-buffer'
  Parsed request is a module
  using description file: /home/carmenmarquez/proyectos/UNIFICACION/SUBIDA/plugins/API-IDEE_CommunityPlugins/plugins/catalogsearch/node_modules/safe-buffer/package.json (relative path: .)
    Field 'browser' doesn't contain a valid alias configuration
    resolve as module

Motivo: al cambiar de versión de Webpack ya no se incluye la librería stream.

Solución: en el caso del plugin que se estaba tratando el módulo buffer es necesario porque timers-browserify depende de safe-buffer.
- Instalar la librería: npm install --save-dev buffer
- Añadir en los ficheros Webpack dentro de fallback:
buffer: require.resolve('buffer/'),


4. error  'M' is not defined  no-undef

Motivo: cambio de nombre de la variable M por IDEE.

Solución: sustituir "M." por "IDEE." en todos los ficheros.

5. Map.js:3827 TypeError: Cannot read properties of undefined (reading 'on')
    at Panel.js:416:17
    
Motivo: en API-IDEE no se permite añadir controles nulos a paneles.
Ejemplo: 
this.panel_.addControls([control, undefined]);

Solución: no añadir controles indefinidos, nulos...


6. Problemas con visualización de plugins que no dispone de panel como tal, es sólo el círculo pero se muestra cuadrado.

Motivo: problemas con el CSS.

Solución: añadir al botón del plugin la regla CSS
border-radius: 20px;


7. El plugin no se añade por API-REST y no da error.

Motivo: el fichero api.json no está configurado correctamente.

Solución: en el atributo name se debe especificar el nombre en minúsculas completo.


8. Se genera en dist el versionado de el fichero LICENSE

Motivo: xml2js que se incluye/empaqueta en el bundle final.

Solución: añadir a los ficheros webpack production dentro de TerserPLugin:
extractComments: false


9. Error: page.evaluate: TypeError: IDEE.plugin.XXX is not a constructor

Motivo: los ficheros js no están bien definidos.

Solución: añadir el @module al inicio del plugin con la definición correcta.
Ejemplo: 
/**
 * @module IDEE/plugin/XXX
 */


10. Error: Access to XMLHttpRequest at '<URL>' from origin 'http://localhost:6123' has been blocked by CORS policy: The 'Access-Control-Allow-Origin' header contains multiple values '*, *', but only one is allowed.

Motivo: el servidor no está configurado correctamente para permitir peticiones CORS desde el origen indicado.

Solución: activar/configurar el proxy para evitar problemas CORS.


11. Error: AppendChild on null (referente al contenedor)

Motivo: al cambiar de Mapea a API-IDEE el método appendChild no funciona ya que el contenedor cambia.

Solución:
- Sustituir en los contenedores mapea por api-idee.
 EJ: 'div.m-mapea-container' por 'div.m-api-idee-container'


12. ERROR constructor sin new
Uncaught (in promise) TypeError: Class constructor a cannot be invoked without 'new'
    at Control.addTo (Control.js:33:24)
    at Control.js:124:14

Motivo: al cambiar de Mapea a API-IDEE la herencia de clases ha cambiado.

Solución:
- Modificar el constructor para que contenga new 
EJ: Cambiar
    ol.control.Control.call(this, {...})  olMap.addControl(olControl); 
	por
    const olControl = new ol.control.Control({ ...})  olMap.addControl(olControl);


13. ERROR  Can't resolve 'assert' 
Module not found: Error: Can't resolve 'assert' 
BREAKING CHANGE: webpack < 5 used to include polyfills for node.js core modules by default.
This is no longer the case. Verify if you need this module and configure a polyfill for it.

If you want to include a polyfill, you need to:
	- add a fallback 'resolve.fallback: { "assert": require.resolve("assert/") }'
	- install 'assert'
If you don't want to include a polyfill, you can use an empty module like this:
	resolve.fallback: { "assert": false }

Motivo: Si una dependencia necesita el módulo `assert` de Node.js, pero no está disponible en el entorno del navegador, se debe configurar un polyfill o desactivar la dependencia.

Solución:
- Añadir en el archivo de configuración de webpack la siguiente línea en el apartado de resolve.fallback:
      assert: false,